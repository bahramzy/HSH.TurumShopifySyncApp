name: Daily Run

on:
  schedule:
    - cron: '0 0 * * *'    # once a day at midnight
  workflow_dispatch:
jobs:
  run:
    runs-on: windows-latest  # changed from ubuntu-latest
    timeout-minutes: 35

    steps:
      - uses: actions/checkout@v4

      - name: Install NuGet
        uses: nuget/setup-nuget@v2

      - name: Restore packages
        run: nuget restore ./HSH.TurumShopifySync/HSH.TurumShopifySync.csproj

      - name: Build app
        run: dotnet build ./HSH.TurumShopifySync/HSH.TurumShopifySync.csproj --configuration Release

      - name: Run app
        run: dotnet run --project ./HSH.TurumShopifySync --configuration Release
        env:
          MY_API_KEY: ${{ secrets.MY_API_KEY }}
